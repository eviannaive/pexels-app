import { Loading } from '@/components/Loading';

export default function loading(){
  return (
    <div className="fixed top-0 left-0 text-5xl w-full h-full flex justify-center items-center"><Loading /></div>
  )
}